import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF4CAF50);
  static const Color secondary = Color(0xFF8BC34A);
  static const Color background = Color(0xFFF1F8E9);
  static const Color accent = Color(0xFF795548);
  
  // Additional helpful colors for text and UI elements
  static const Color textPrimary = Color(0xFF2E312F);
  static const Color textSecondary = Color(0xFF6B726D);
  static const Color error = Color(0xFFE57373);
  static const Color surface = Colors.white;
}
